@model IEnumerable<EpiManager.Models.PriceHeader>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PriceName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PriceName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PriceHeaderId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PriceHeaderId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PriceHeaderId })
        </td>
    </tr>
}

</table>
